{% macro configWarning(setting) -%}
    {% set setting = '<code>'~setting~'</code>' %}
    {{ 'This is being overridden by the {setting} config setting in your {file} config file.'|t('elasticsearch', {
        setting: setting,
        file: 'elasticsearch.php',
    })|raw }}
{%- endmacro %}
